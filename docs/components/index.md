# Components

The platform is composed of independently versioned repositories. Use the
component pages to enter generated documentation. Private source links require
authorization and include an email path for requesting access.

The portal assembles the same sites at stable URL prefixes so cross-component
links remain predictable. [View every discovered component](discovered.md).
